# parked — 不在包里

这里的东西**不安装,也不在 Skill 发现路径上**。它们不是坏的,是**没有被真实用过**,
所以不该占据常驻的选择空间——十二个 description 每次会话都常驻,每多一个都在稀释
其余的触发准确率。

| | 为什么在这 |
|---|---|
| `thinking-toolkit` | 2026-08-25 建,真实调用一次(还是写它的同一个会话里) |
| `life-design-interview` | 2026-08-25 建,零调用 |
| `project-doc-system` | 从没有任何项目按它建起过完整文档体系 |

**规矩**:真实任务里需要它,就搬回 `skills/`;三个真实任务过去都没搬回来,就删掉。
不设第三种状态,也不"降级后继续养"。
