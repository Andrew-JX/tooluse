# 交接常见假绿灯

- 服务端直连探针通过，被当成客户端实际参数路径已验证。
- 35 条断言打印通过，但命令始终不退出；断言通过不等于资源已清理。
- 只回退配置，没回退被断言保护的条件；测试仍绿，目标分支一次也没执行。
- `git push` 成功输出被当成远端状态；应以 `git ls-remote` 核目标 SHA。
- 「全部修完」没有枚举，实际只关了一部分 finding。

这些案例说明需要的证据形状，不替代当前契约。
