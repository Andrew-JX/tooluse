# mj-portfolio · tooluse 流程治理批次契约

> 规划者：Claude ｜ 执行方：Codex ｜ 2026-08-08
> 本文件是第⑤步的产物。开工前由用户批准并提交，提交后即 `contract SHA`。
> 判据一经冻结，执行方不得新增、删除、放宽或改写。

**作废声明**：`1c82430` 作废，以本文件的最新提交为准。
**修订经过**：见 `tooluse/CHANGELOG.md`，本文件不复述。

---

> **本契约已履行完毕，其中 P0-1 于 `c175400` 被推翻，判据 6 随之永久不可满足。**
>
> P0-1 要求新建 `tooluse/CHANGELOG.md` 作为流程变更账本，判据 6 是它的机器验收行。
> `c175400`（瘦身：12 → 8 个 Skill）删除了该文件，现行规则改为 [CONTRIBUTING.md](../CONTRIBUTING.md)
> 第 3 条「改动记在 commit message 里，不另设变更账本——同一件事记两处，其中一处一定会先烂」。
> 上面「修订经过见 CHANGELOG.md」这一行同样失效。
>
> 冻结的判据不回改，所以正文一个字不动；推翻只在这里声明。**引用本文件任何一条之前先读这段。**
>
> `contracts/` 是历史记录，不是现行规则。当前规则在 [README](../README.md) 与 CONTRIBUTING。
> 没有门禁守这一点——`self-check.sh` 九项不覆盖 `contracts/`，而按 CONTRIBUTING 第 1 条不再为此新增脚本。
> 这是明知的取舍：一份 1 个文件的集合，靠这段声明比靠一条门禁便宜。

判据表里的所有计数均由规划者跑命令取值，未凭记忆写。数值与取值方式只在判据 4、5 里定义，本处不复述。

---

## 0. 开工前必须先做的三件事（顺序不能变）

这批本身要检验三锚点规则可不可用，所以从这里开始就按它走。

**0a. 冻结上一批。** 当前工作区有上一批（skill 修订 + kind/origin 分组 + work-skill 移植）的未提交改动。先 `git add -A` 再单独提交，提交信息写清楚是上一批。

> 注意：`tooluse/skills/evidence-led-reviewer/` 下有文件处于 `AM` 状态（暂存的是旧版）。必须 `git add -A` 之后用 `git diff --cached` 确认 `SKILL.md` 里含 `ADVISORY` 和 `contract SHA` 两个词，再提交。直接 commit 会提交修改前的版本。

**0b. 把本文件放进仓库并单独提交。** 路径固定为 `tooluse/contracts/2026-08-08-hardening.md`。这一个提交只含这一个文件。

**0c. 记录三锚点。**

```
contract SHA = 0b 的提交
baseline SHA = contract SHA（本批实现从契约提交之后开始，两者取同一个值）
candidate SHA = 实现提交，开工时为空
```

如果 0a 或 0b 任一步被拒绝执行，停下报告，不要开始 §2 的任何改动。

---

## 1. 硬约束

- 唯一机器验证命令是 `npm run build`。仓库没有 lint 和测试。
- 本地提交已授权。**push、merge、tag、发布仍未授权。**
- 构建通过不等于页面正确。视觉与交互由用户人工确认，执行方不得代为判定。
- 页面只有一个滚动条，不允许内层滚动容器。
- 文案里不出现具体 agent 产品名，不出现与用户的对话内容。
- 本批不改任何 skill 的 `status` 字段。四个自建 skill 全部维持现状（3 个尚未实测 + work-skill 尚未实测）。**没跑过真实批次之前不许升级状态。**

---

## 2. 改动清单

### 允许改动的文件（实现阶段严格等于下面这份清单，数量以清单为准）

```
tooluse/CHANGELOG.md                        （新建）
tooluse/README.md                           （新建）
tooluse/skills/acceptance-author/SKILL.md
tooluse/skills/evidence-bound-executor/SKILL.md
tooluse/skills/evidence-led-reviewer/SKILL.md
src/data/deliveryFlow.zh.ts
AGENTS.md
```

`tooluse/contracts/2026-08-08-hardening.md` 已在 baseline 之内，实现阶段不得改动，因此不在上面的清单里。判据 2 的比对结果里出现它，即为未达成。

**`src/data/toolShares.zh.ts` 和 `src/styles/app.css` 不在清单里。** 本批不新增工具条目、不新增边类型，所以这两个文件不应该有任何改动。如果你发现必须改它们，停下报告。

---

### P0-1 · 新建 `tooluse/CHANGELOG.md` — 流程自己的变更账本

**为什么**：这套流程要求每个批次有契约和轮次账本，但流程自己被改过三轮，没有任何记录说明哪次改动过了什么门、依据是什么。想回答「⑦ 的 ADVISORY 规则为什么是这样」，答案现在散在几个聊天窗口里。

这是同一个病的第二次发作——上一次是独立审查拿着旧契约把已授权改动判成违约，根因同样是「事实活在对话里，没进仓库」。

**内容要求**：

文件头写下面这组维护规则，一条不少：

```
1. 这套 skill、交付流程和批次契约的每一次改动，在这里记一行。改动和记录在同一个提交里。唯一例外：为建立 `contract SHA` 而单独冻结的契约提交只含契约文件，它的变更记录在紧随其后的实现提交里补记，并引用该 `contract SHA`。
2. 这里是文档演化记录的唯一去处。被改的文档本体不留自己的修订经过——「某处从 X 改成了 Y」描述的是旧状态，文档一变它就成了陈旧引用。本体只留仍然生效的东西（比如哪个 SHA 作废）。
3. 修剪规则：连续三个批次没有任何一次被触发过的条目，降级到 references/ 或删除，并在本文件记一行。只增不减的流程最终会重到没人遵守。
```

然后是表格，列固定为：

| 日期 | 改了什么 | 为什么（依据哪次事故 / 谁的建议） | 提出方 | 过了什么门 |

**必须回填下面每一轮已经发生的改动**，日期从 `git log` 取，不要凭记忆写：

- `evidence-led-reviewer` 首次建立（P0–P3 分级、证据契约、跨工具模板）
- 新增 `acceptance-author`、`evidence-bound-executor`、`blind-spots.md`，并对 reviewer 打 8 处补丁（前置条件 / 先审判据 / 六行事实 / 全称检查 / 隔离回退 / ADVISORY / 判据错不进 P0–P3 / 最没把握的判断）
- kind + origin + categoryId 数据模型、按 kind 分组、work-skill 移植、三条新边、CI 文案区分见证与门、查询参数合并修复
- 本契约在第⑤步被执行方反复退回的经过：每一轮的退回理由与改法，逐轮记录，轮数以实际发生为准（已知类型有：判据数字凭记忆写错、交接格式跟着判据表漏改、契约内同一事实重复导致改源头漏改引用、契约自身规则互相打架）。契约本体不留这段记录，它的唯一去处就是这里

「过了什么门」一栏如实写当时的情况，包括「无」。不要为了好看编一个门。

---

### P0-2 · 跑规则冲突检查，并修掉已知的那一处

**为什么**：`acceptance-author` 第 8 条叫「判据之间不许打架」，但没人对这四份 skill 自己跑过这一条。已经能看到一处结构性死锁。

**已知冲突**：

- `evidence-bound-executor` 第 6 条：上一批没有形成获授权的冻结 candidate 时不要开始下一批；用户未授权提交时写「未收口」并等待。
- `acceptance-author` 冻结规则：先冻结 `contract SHA` 与 `baseline SHA`，再开始实现。

在一个禁止提交的批次里，这两条合起来意味着**既不能开工也不能收口**。上一批就是这个状态。

**修法**（两份文件都要改，措辞自拟但必须含这三层）：

1. 本地提交（不 push）默认视为已授权，这是三锚点的前提。
2. 确实不能提交时，用契约文件的内容哈希加绝对路径作为 contract 锚点的替代；审查方比对哈希。
3. 这种情况下批次状态封顶为「⑥完成，未收口」，⑦只能给 `ADVISORY`，不得给 PASS。

**同时**：拿 `acceptance-author` 第 8 条通读四份 SKILL.md（含 work-skill）与 `blind-spots.md`，找还有没有别的冲突。找到就在交接里列出来并给修法建议，**不要自行修**——除非那处冲突和上面这条属于同一根因。

---

### P0-3 · 仓库规则加保鲜机制

**为什么**：④ 的 skipWhen 是「仓库里已经有一份还准确的规则文件」，但没有任何东西验证它还准确。规则烂掉是静默的：新窗口照着烂规则干活，全链路绿灯。这个仓库和 FitMind 都已经有过文档与实现不一致的实例。

**改 `AGENTS.md`**：文件开头加一行（日期取本批实际日期）：

```
> 最后复核：<YYYY-MM-DD> · 超过 90 天或 5 个批次未复核，第④步不得跳过，必须逐条对照当前仓库事实重新确认。
```

**改 `src/data/deliveryFlow.zh.ts`** 中 `id: 'repo-rules'` 的 `skipWhen`：

```ts
skipWhen: '仓库里已经有一份规则文件，且它的「最后复核」日期还在有效期内。过期就不许跳过——规则烂掉是静默的，照着烂规则干活会全链路绿灯。',
```

---

### P0-4 · `acceptance-author` 第 3 条补齐「数字与事实的单一来源」

**为什么**：这份契约在第⑤步被执行方反复退回，全部是规划者的判据错误，根因集中在三种形态——数组条目数和文件数凭记忆写、没跑过命令取值；同一个事实在文中写了多份，改了源头漏改引用；连修订记录本身都变成了指向不存在数值的陈旧引用。

`acceptance-author` 第 3 条已经要求「度量必须给出算法」，但「数组长度」在一个含三个数组的文件里不是算法。规则存在，没人对这份契约跑过。

**改 `acceptance-author/SKILL.md` 第 3 条**，末尾追加：

```
- 任何写进判据的具体数字（条目数、文件数、行数、耗时、阈值）必须在冻结前实际跑一遍命令取值，并在判据里写明取值命令。凭记忆写数字是本流程已经发生过的假绿灯来源。
- 计数类判据要写清数的是哪个集合。同一份文件里有多个数组时，「数组长度」不构成算法。
- 同一个事实（条数、文件数、模板、清单）在契约里只写一处，其他位置引用它而不复述数值。复述出来的每一份都是一个会陈旧的副本——本流程的判据退回，根因全部在这里。
- 确实需要改动某个数值时，改完把契约里引用它的每一处都搜一遍再冻结。
- 契约里不写自己的修订经过。「本文件某处从 X 改成了 Y」描述的是文档的旧状态，文档一变它就成了新的陈旧引用。修订经过进变更账本，契约本体只留一行仍然生效的作废声明（哪个 SHA 不再有效）。规则的**理由**（哪次事故导致了这条）不属于此列，那是关于世界的事实，不会陈旧，留着。
```

**约束**：只改第 3 条，该 skill 其余各条的编号和内容一律不动。

**生效范围**：这些规则从**下一份契约**起生效。本契约是引入它们的那一份，不对自身回溯适用——一条关于「怎么写契约」的规则写在契约里，再拿它审自己，会无限递归。本契约已知的不符之处：`§0b`「契约提交只含契约文件」与 §2 的允许改动清单在结构上是两套口径（前者管冻结提交，后者管实现提交），本批不统一，下一份契约里合并。发现其他不符，记进变更账本，不在本批处理。

---

### P1-1 · ⑨ 加裁决超时

**为什么**：⑨ 现在没有降级模式。如果一周不裁决，这个批次怎么办，没有定义。按晚上加周末的节奏这一定会发生。

`id: 'verdict'` 的 `pitfall` 末尾追加一句：

```
还有一头没人管：超过 14 天没裁决，这个批次要转 stale——SHA 还在，但依赖、环境和我自己的上下文都变了，必须重新核验，不能直接沿用旧证据继续裁决。
```

---

### P1-2 · ⑦ 的有效性来源要写对

**为什么**：⑦ 现在的文案只说了「换角色名不等于独立」（否定式）。它没说这一步为什么仍然有用。在同一台机器、同一个权限域下，身份是装不出来的——已经证伪过。但第二次阅读确实抓到过大量真实缺陷，那个效果不来自身份，来自读的人手上没有第一次的叙述。

按现在的写法，容易被读成「这一格是走过场」。

`id: 'verify'` 的 `pitfall` 末尾追加：

```
但也别把这一格当摆设：它有效不是因为身份隔离——同权限同机器装不出身份——而是因为第二次阅读的人手上没有第一次的叙述。所以真正要守的不是「换个人」，是「审查方拿到的必须是仓库事实，不是执行方的摘要」。
```

---

### P1-3 · 交接格式加时间分段

**为什么**：修剪规则要能执行，前提是知道每一步的成本。用户 §8 那条判据（一个批次从开工到 verdict 的墙钟时间 + 打回轮数）到今天是空的，因为两次批次都没走到 verdict。没量过就没法剪。

`evidence-bound-executor` 的交接格式模板里加两行：

```
本批墙钟时间：⑤ _ 分 / ⑥ _ 分 / ⑦ _ 分 / ⑧ _ 分 / ⑨ _ 分
打回轮数：审查退回 _ 轮 / 判据回退 _ 轮
```

---

### P1-4 · 审查输出记录哪些规则真的触发了

**为什么**：同上，修剪规则的输入。

`evidence-led-reviewer` 输出格式的结尾清单里加一项：

```
- 本次真正触发过的规则：（skill 名 + 条目号。没触发的不要列，列了就失去意义）
```

---

### P2-1 · 新建 `tooluse/README.md` — 这个库是什么，以及它的已知边界

**为什么**：这些 skill 可能被公开、被复制到别的项目。一份不说明自己边界的规则集，比没有规则更危险——使用者会以为它覆盖的比实际多。

**必须包含四部分**：

**一、这是什么。** 四个 skill 各一行，加安装路径（`.claude/skills/` 和 `.codex/skills/`），以及不支持 skill 的工具用哪份模板。

**二、状态词的含义。** 和 `toolShares.zh.ts` 里那五个状态一致。明确写：四个自建 skill 目前全部「尚未实测」，没有在真实批次上跑过。

**三、已知边界（这一节最重要，如实写，不要软化）**：

- **闭环**：规则的输入全部来自同一个人的项目、窗口和记忆。这套体系可以带着完整证据链整体一致地错，而系统内几乎没有信号源能发现。唯一验证过有效的外部输入是「用真实数据使用运行中的产品」。
- **幸存者偏差**：每条规则都对应一次被抓住的事故。没被抓住的事故不产生规则，也没有机制让它们浮出来。`blind-spots.md` 是形状级泛化，是对这条的部分缓解，不是解决。
- **依从性未验证**：agent 侧靠 skill 自动加载，人侧的第①步和第⑨步没有任何触发器，纯靠人想起来——而这两步恰好是唯一防「判据本身写错」的两步。
- **触发准确率未测**：`description` 写坏的 skill 要么永不触发要么处处触发，两种都没量过。

**四、不适用范围。** 一晚上内、单模块、可回滚且不涉及安全 / 数据 / 权限 / 发布的小活，四个 skill 都不触发。

**约束**：这份 README 是给使用者看的，不是开发日志。不要写批次号、契约路径、和用户的对话。

---

## 3. 验收判据（下表全部，标注验证类型，冻结）

| # | 类型 | 判据 | 度量 |
|---|---|---|---|
| 1 | 机器 | `npm run build` 退出码为 0 | 跑一次，贴退出码 |
| 2 | 机器 | 改动文件集合严格等于 §2 的清单 | `git diff --name-only <baseline-SHA>` 与 `git ls-files --others --exclude-standard` 取并集，逐字比对 §2 清单。多一个少一个都算未达成。契约文件在 baseline 内，不应出现在结果里 |
| 3 | 机器 | `src/data/toolShares.zh.ts` 与 `src/styles/app.css` 零改动 | 上一条的并集里不含这两个路径 |
| 4 | 机器 | `toolShares` 仍是 15 条，四个自建 skill 的 `status` 一字未改 | `git diff <baseline-SHA> -- src/data/toolShares.zh.ts` 输出为空 |
| 5 | 机器 | `flowNodes` 仍是 10 条，`flowEdges` 仍是 6 条 | 只数对应数组内的条目，`flowScales` 的 3 条不计入。交接里写出两个数字 |
| 6 | 机器 | `CHANGELOG.md` 含 §P0-1 代码块里的每一条维护规则；§P0-1 回填清单里的每一轮改动都能查到，每条记录的日期都能对上一个真实 commit | 逐条 grep 维护规则的首句；行数不限，可合并可拆分；交接里逐轮给出「内容 → 日期 → commit」的对应。查不到对应 commit 的轮次，如实写「无对应提交」，不许编日期 |
| 7 | 机器 | `AGENTS.md` 首部含「最后复核」和 90 天 / 5 批次的复核规则 | grep |
| 8 | 机器 | `deliveryFlow.zh.ts` 中 `repo-rules`、`verdict`、`verify` 三个节点的文案已按 P0-3 / P1-1 / P1-2 改动 | 逐个 grep 关键词：「最后复核」「14 天」「第二次阅读」 |
| 9 | 机器 | executor 交接模板含墙钟时间与打回轮数两行；reviewer 输出清单含「本次真正触发过的规则」 | grep |
| 10 | 机器 | `README.md` 的已知边界一节含四条，且四个自建 skill 均标为尚未实测 | 逐条点名 |
| 11 | 人工 | 打开 tooluse 页面，⑦⑨④ 三个节点的新文案没有撑破卡片或产生内层滚动；1280px 和 375px 各看一次 | **验收者是用户。执行方不得自行判达成，只能写「未验证，待用户确认」** |
| 12 | 机器 | 冲突检查已跑完：四份 SKILL.md 加 `blind-spots.md` 全部通读，已知那处冲突已修，其余发现逐条列出未自行修改 | 交接里给出通读证据和发现清单；无其他冲突就明确写「无」 |
| 13 | 机器 | `acceptance-author` 第 3 条已把 P0-4 代码块里的每一条都加进去，一条不少；该 skill 其余各条编号与内容未变 | 逐条 grep P0-4 代码块中每一条的首句；`git diff` 确认除第 3 条外无其他改动 |

**限定词检查**：

- 「每一轮改动」= §P0-1 回填清单里列出的那几条，不多不少；记录成几行不限。
- 「四条已知边界」= §P2-1 三之下的四个小标题，不多不少。
- 「三个节点」= `repo-rules` / `verdict` / `verify`。
- 「14 天」「90 天」「5 个批次」是本契约指定的固定值，不是建议值，不许自行调整。

**已知的假绿灯**（写在这里，是为了防止它发生）：

- 判据 2 如果只跑 `git diff` 不跑 `git ls-files --others`，新建的三个文件不会出现在结果里，会误判达成。上一批就是因为文件集合没枚举，把 447 行别的批次的改动放行了。
- 判据 11 如果用「构建通过所以排版没问题」推理，就是从属性推行为。必须是用户人眼。

---

## 4. 交接格式

```
contract SHA：
baseline SHA：
candidate SHA：

改动文件（逐个列出，tracked 与 untracked 分开）：
源码 +N/-M        测试文件 +N/-M（本仓库无测试，写 0）
未跟踪文件：

§3 表格里的每一条逐条：达成 / 未达成 / 未验证 + 证据
（标「人工」的那条只能是「未验证」）

规则冲突检查结果：已修 1 处 / 另发现 N 处（逐条列出）/ 无其他
全称声称及其穷举命令：（没有就写「无」）
墙钟时间与打回轮数：按 P1-3 代码块的两行填写
  （⑧ 本仓库无 CI，写 0 并注明「空门」；⑨ 由用户裁决，执行方留空）
范围外发现、已记录未做：
最没把握的判断：
```

---

## 5. 不归执行方的三件事

1. **人眼看页面**，给判据 11 结论。
2. **给出 PASS 或 CHANGES_REQUESTED**。这次要真的走到⑨，前两批都没走到。
3. **测触发准确率**：本批之后，另起一个日常任务，什么都不说，看三个 skill 会不会自动加载。这是第①条已知边界唯一的验证方式，执行方做不了。

---

## 6. 本契约的核验边界

- P0-2 那处规则冲突是我读两份 SKILL.md 推出来的，没有实际跑一个批次撞到它。我认为它成立，但这是推断。
- 14 天 stale、90 天复核、连续三批未触发即修剪——这三个阈值是我拍的，不是从你的历史数据算出来的。跑几个批次之后按真实情况调。
- 「第二次阅读有效」这个判断，依据是那条九轮弧线里约一半 finding 来自审查方窗口。我没有独立核验那份统计。
- 我没有读 `work-skill/references/` 下的 b-stuck、c1、c2 三份，所以 P0-2 的冲突检查在这三份上可能有遗漏。
