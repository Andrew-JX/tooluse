# Tooluse 变更账本

1. 这套 skill、交付流程和批次契约的每一次改动，在这里记一行。改动和记录在同一个提交里。唯一例外：为建立 `contract SHA` 而单独冻结的契约提交只含契约文件，它的变更记录在紧随其后的实现提交里补记，并引用该 `contract SHA`。
2. 这里是文档演化记录的唯一去处。被改的文档本体不留自己的修订经过——「某处从 X 改成了 Y」描述的是旧状态，文档一变它就成了陈旧引用。本体只留仍然生效的东西（比如哪个 SHA 作废）。
3. 修剪规则：连续三个批次没有任何一次被触发过的条目，降级到 references/ 或删除，并在本文件记一行。只增不减的流程最终会重到没人遵守。

| 日期 | 改了什么 | 为什么（依据哪次事故 / 谁的建议） | 提出方 | 过了什么门 |
|---|---|---|---|---|
| — | 首次建立 `evidence-led-reviewer`：P0–P3 分级、证据契约与跨工具模板 | 需要把审查结论、证据要求和跨工具角色固定下来 | 用户与执行方 | 无对应提交；只存在于冻结前的浮动工作区，没有独立门禁 |
| 2026-08-08 | 新增 `acceptance-author`、`evidence-bound-executor`、`blind-spots.md`，并补齐 reviewer 的前置条件、先审判据、六行事实、全称检查、隔离回退、ADVISORY、判据错误分流和最没把握判断 | 多轮假绿灯、浮动工作区审查和判据本身写错的事故 | 用户、规划者与独立审查 | `dd56ff6`；`npm run build` 通过，独立审查因浮动工作区只能给 ADVISORY |
| 2026-08-08 | 增加 kind / origin / categoryId 数据模型、按 kind 分组、移植 work-skill、增加三条特殊边、区分 CI 见证与门，并修复查询参数合并 | 工具与方法混排、工作流程缺入口、回路不可见和参数覆盖问题 | 用户与规划者 | `dd56ff6`，后并入 `df4d2f4`；构建与浏览器机械核验完成，最终视觉仍由用户裁决 |
| 2026-08-08 | 流程治理契约在第⑤步被执行方反复退回：修正凭记忆写错的计数、补齐交接格式、去除契约内重复事实，并解决契约冻结提交与变更账本同提交规则的冲突 | 冻结前没有实际取数，同一事实在契约中存在多个会陈旧的副本 | 规划者；执行方按冻结规则退回 | contract `3acdf0d`；在实现开始前完成，未消耗源码返工 |
| 2026-08-08 | 完成首个走完十格的流程治理批次：建立变更账本、修复契约冻结死锁、增加规则保鲜与修剪机制，并公开流程的已知边界 | 用真实批次检验三锚点、独立审查和用户裁决能否完成闭环 | 用户、规划者、执行方与独立审查 | contract `3acdf0d` → candidate `4b8a1b8`；`npm run build` exit 0；⑦正式审查通过，审查退回 0 轮；⑨用户人工裁决 PASS（1280px / 375px 各验一次）；判据回退 4 轮；墙钟 ⑤ 693 分 / ⑥ 7 分 / ⑧ 0 分（空门） |
| 2026-08-25 | 在 `acceptance-author` 冻结规则加入窄版逃生阀：只有本契约新引入的「契约编写元规则」不回溯适用于引入它的这一份；既有规则仍必须检查当前契约。不得笼统写「新规则下批生效」，避免形成绕过判据的漏洞 | 契约元规则审自己会产生无限递归；本批判据回退中有两轮由此造成 | 规划者与执行方 | 本行所在提交（提出于 2026-08-08，压 17 天）；落为 `## 冻结规则` 下的 `### 元规则的窄版逃生阀`，写死三条不得扩大的边界：不许笼统写「新规则下批生效」（那会连产品判据一起豁免）、只覆盖关于契约写法的规则、分不清时按不豁免处理，并要求用掉时在契约里写明用在哪一条上 |
| 2026-08-25 | 把「允许改动文件按阶段拆开」从一次性叮嘱升为常驻规则：冻结阶段清单与实现阶段清单分别定义，文件集合判据引用对应阶段 | §0b 与 §2 现在是同一件事的两个独立口径 | 执行方 | 本行所在提交（提出于 2026-08-08，压 17 天）；原文写的是「下一份契约」——那是一次性叮嘱，只对紧接着那一份生效，之后就没人记得，因此改写进判据 4 标题与正文成为常驻规则，并同步 `## 输出格式` 模板：单行「允许改动文件」拆成冻结阶段与实现阶段两行，后者显式标注不含契约文件 |
| 2026-08-25 | `evidence-led-reviewer` 新增 `## 逐轮记录`：轮次编号、findings 批次内唯一编号、上一轮逐条结账、审查退回与判据回退分别计数 | 规则写在流程图⑦，但审查方加载的是 skill，该规则对它不存在 | 独立审查 | 本行所在提交（提出于 2026-08-08，压 17 天）；两类回退强制分别计数——合并之后看不出是判据在错还是实现在错，而这正是本仓库历史上最贵的一类混淆。**根因未解**：见下一条待办，流程图本身不在本仓库 |
| 2026-08-25 | README 新增「交付流程的步骤编号」一节，复制 ①–⑪ 的编号与标题；三个交付 Skill 里 6 处裸编号就近加上步骤标题（`第⑦步` → `第⑦步（分级核验）`） | 三个交付 Skill 引用了流程图步骤，而流程图在 `mj-portfolio/src/data/deliveryFlow.zh.ts`，本仓库 0 处定义：只拿到本仓库的人无法解析「第⑦步」「⑥完成，未收口」。用户指出定义所在后取得原文。同时做两件事而非一件——README 表解决「分享整个仓库链接」这条路径（用户的实际主用法），就近标注解决「Skill 按目录单独安装」这条路径，后者拿不到 README | 独立审查（2026-08-08 提出）与用户（2026-08-25 指出定义位置） | 本行所在提交；定义取自 `Andrew-JX/mj-portfolio` 的 `src/data/deliveryFlow.zh.ts`，实际为 **11 步（①–⑪）而非账本 2026-08-08 行所写的「十格」**——那是当时的口径，彼时尚无第⑪步，账本不追溯改写，改由 README 标注差异。README 那张表是**有意副本**，已在节内写明权威在那份文件、标题变了要同步，与 `reviewer-role-prompt.md` 用同一套副本纪律。`Andrew-JX/vault` 经核查为 **404（私有或已改名）**，未能读取，不作为来源。剩余裸编号只在墙钟耗时模板与已加标注行的重复位置，语境自明，未再改 |
| 2026-08-09 | 把 `work-skill` 的唯一可安装源码迁到私人本地工作系统，用户级目录联接改指新源；本站删除物理 Skill 包但保留公开页面说明 | 原始完整规则、拆卡投影和本站精简发布包分处两地，继续并行维护会产生互相陈旧的副本 | 用户与执行方 | contract `f3129ba`；DIP baseline `1698106` → candidate `79b46d6`；mj candidate 为本行所在提交；私人 log 未进入 Git |
| 2026-08-10 | 新增 `deploy-static-site-to-tencent`：参数化脚手架、GitHub Actions、受限 SSH 发布脚本、密钥安装器、运行手册与 Tooluse 条目 | 从本站腾讯云真实上线中提炼；首次 Actions 上传因 SSH 管道短读截断 gzip，加入 `dd iflag=fullblock`、归档校验、原子切换与失败回滚，避免后续项目重复踩坑 | 用户与执行方 | 本行所在提交；官方 `quick_validate.py`、脚手架生成、拒绝覆盖、非法输入、生成 YAML 解析、ShellCheck 与 `npm run build` 均通过；底层部署证据为 Actions run `31398012907`，不等同于 Skill 已实测；页面视觉未验证 |
| 2026-08-10 | 从 `mj-portfolio/tooluse` 拆出独立仓库，并删除 `deploy-static-site-to-tencent` 未实测的生成器与三份实现模板，改为跨项目安全判据清单和两套参考实现 | 首个全栈客户仓库没有复用静态站模板，而是独立重写了 server-only `.env`、数据库和按授权分支指定 SHA 发布的形状；模板层复用测试为 0/1，真正迁移的是安全判据 | 用户与执行方 | 本行所在提交；全局 `core.autocrlf=true` 使编辑器写出的 LF 源工作树与 checkout 得到的 CRLF 新工作树不适合用 `diff -r` 作等价性判据；闸门改为 tree 哈希，`mj-portfolio` 的 `HEAD:tooluse` 与本仓库 `0adb2d4^{tree}` 均为 `fe3541ef48825625e07cd29df8b53f9715a2e296`；官方 `quick_validate.py` 通过，生成器与模板路径已穷举删除；拆分与删模板不构成真实使用证据 |
| 2026-08-11 | 把部署 Skill 改名为 `deploy-to-self-managed-server`，修正静态站名称造成的触发偏差，把未公开的全栈参考降级为不点名的 Shape B，并对一份历史契约做不改变判据含义的项目名去标识化 | 清单已经覆盖静态产物与全栈精确提交两种形状；旧名称会让全栈部署任务漏触发，公开仓库也不应暴露未公开实现的仓库、路径、域名或包管理器指纹 | 用户与执行方 | 本行所在提交；旧名称仅命中历史账本，未公开实现名称零命中；Shape B 的严格命令与授权分支、无应用产物或 `.env` 上送、`flock`、服务端迁移、运行版本回滚和隔离测试均有源码对应，版本专属健康门未实现并已明确标成缺口；官方 `quick_validate.py` 通过；改名和安装不构成真实使用证据 |
| 2026-08-11 | 撤回上一行的去标识化：Shape B 恢复点名 `Andrew-JX/FitMind_ai` → `fitmind-ai/deploy/scripts/`，与 Shape A 对称；恢复冻结契约 `2026-08-08-hardening.md` 中被改写的项目名；同时把「Shape A 校验 release marker」改写为「校验每个版本都有的内容标记」，并新增「判据 9 是两套参考实现都不满足的唯一一条」 | 上一行的降级依据是「该仓库未公开」，核实后 `Andrew-JX/FitMind_ai` 为 PUBLIC，该前提作废，正文因此留下一句可证伪的假陈述；冻结契约不应因后续决定被追溯改写；Shape A 原描述把非版本专属的内容标记写成 release marker，属于同一处事实高估 | 用户裁定与执行方 | 本行所在提交；上一行保留不改写，由本行记录前提被证伪与回退范围；`grep -rni fitmind` 现仅命中参考实现节与冻结契约原文，旧 Skill 名称仍仅命中历史账本；官方 `quick_validate.py` 通过；判据 9 的两处缺口为只读核对结论，尚未在任一项目修复 |
| 2026-08-11 | 新增 `project-doc-system`：四份起始集脚手架、路由式索引、按触发条件生长的文档目录、每批留痕账本、通用索引门禁与门禁移植指引 | 文档腐烂只能靠机器门禁和触发条件控制，而不能靠「每做一件事写一份文档」——真实项目的复盘显示 43 份文档 13,736 行时密度已偏高，重复副本是最大漂移源，快照型文档会指挥人去修改正确的实现；同时冷启动需要一个不读全仓就能定位的入口，否则上下文成本随项目规模爆炸 | 用户与执行方 | 本行所在提交；`init-docs.mjs` 在隔离空项目建出四份文件并二次运行全部跳过，对 `mj-portfolio` 与 `fitmind-ai` 只读探测分别报出 1 条与 3 条已触发的生长条件，账本超 500 行的归档条件在 611 行时触发（切分轴不强制，按批次、功能或时间随项目定）；`check-doc-index.mjs` 4 项检查通过、4 个负向控制全部「被破坏时确实报错」；官方 `quick_validate.py` 通过；没有任何真实项目按它建起过文档体系，脚手架跑通不等于 Skill 已实测 |
| 2026-08-17 | 撤回 2026-08-09 那一行：`work-skill` 的唯一源码迁回本仓库 `skills/work-skill`，用户级目录联接改指新位置，README 三处「五个公开 Skill」「work-skill 不从本站仓库分发」的措辞同步更新 | 存放它的私人本地工作系统 `E:\workspace_DIP` 因工作机改为公司设备而整体退役，2026-08-09 依据的那一层已不存在；同时该源码经检索确认不含任何组织、人员、内部路径或平台信息，而它早已在 `mj-portfolio` 的 Tooluse 页面对外展示——把对外展示的工具的源码留在「永不进 Git 远端」的机密层，本身是自相矛盾的分层 | 用户与执行方 | 本行所在提交；`diff -r` 旧源与新位置 0 差异，六个文件 SHA256 逐一记录——这是**工作树口径**，`.gitattributes` 会在入库时把六个文件的 CRLF 规范化为 LF，与其余五个 Skill 一致，入库字节与工作树字节并不相同；组织相关词（公司简称、中文全称、`dip-ai` 域名、内部平台名、人名）在 `skills/work-skill/` 全部零命中，仅命中已删除的旧 README 路径行；用户级联接以 `New-Item -ItemType Junction` 重建，验证 `LinkType=Junction` 且目标可达；私人运行记忆 `log/` 未随迁，仍在工作机本地且从未进入 Git；迁回不构成真实使用证据，状态仍为「尚未实测」 |
| 2026-08-17 | `acceptance-author`、`evidence-bound-executor`、`evidence-led-reviewer` 三个由「尚未实测」升为**正在使用**；README 状态段改写，「依从性未验证」与「触发准确率未测」两节据此更名并补充仍未解决的部分 | `family-finance` 的 1B、2A、2B 三个真实批次首次给出完整证据链，补上了「方法做过，但 Skill 包是否真被触发」这个一直缺的环节；此前状态停留在「尚未实测」是因为只有方法相似性，没有 `SKILL.md` 被读取的直接记录 | 用户与执行方 | 本行所在提交；证据为历史会话归档中各自 `SKILL.md` 的读取记录、紧随其后的合同冻结 / 候选实施 / 独立复核动作，以及对应 contract 与 candidate；三批审查退回 1、6、1 轮，判据回退均为 0；2B 拦下「浏览器断言全过但 Windows 根命令因进程清理超时退出 1」的假绿灯；未把 2026-08-16 的只读审计窗口算进使用次数；升级只覆盖三个交付 Skill，`deploy-to-self-managed-server`、`project-doc-system`、`work-skill` 仍为「尚未实测」；触发准确率仍无未触发对照与固定任务集，端到端有效性仍无统一口径，两项均未随本次升级解决 |
| 2026-08-22 | `work-skill` 由「尚未实测」改为**冻结**，并新增「冻结」状态词；README 状态词定义、状态段与工具列表同步 | 作者入职后在真实工作流里一次也没有把它拿起来过，随它摆渡到工作机的私人运行记忆同样零使用。现有状态词没有一个为真：「已卸载」断言了一次并未发生的移除（用户级目录联接仍指向本仓库），「尚未实测」断言「没有真实批次证据」，而真实条件下零次调用是结论不是空白。徽章是机器可读的那半，让它说假话、把实情藏进 badAt，正是这套流程一直在防的形状 | 用户与执行方（由 `mj-portfolio` 会话裁定） | 事实源是 `mj-portfolio/src/data/toolShares.zh.ts`，本仓库为跟随方；事实源提交 `6bb54bc` → `b4cf7d3` → `a225689`，部署 run 结论 success，线上产物经取回核验（新 chunk 文件名只有本次构建才有，旧文案 0 残留）。该批首次真正执行交付流程第 ⑨ 步（真实设备视觉裁决），实测 1280/375 两档、深浅两态，并抓到一处**批次外**的预存在违规（375px 下 `.site-nav` 内层横向滚动）——这是第 ⑨ 步的首个正面样本。自动化拦下 0，人工拦下 5。耗时全程 51:10，但按「只动单个模块」判据不成立，计入**中档**，小活档耗时仍缺。另记两点：退役一个东西时 grep 其正式名只能找到显式引用，本次三处 `methodNote` 用入口卡代号（A/B/C1/C2）指代，字符串搜索命中不了，改按**别名**扫描才闭合；「冻结」与本仓库既有的动词用法（冻结判据 / 冻结契约）同形不同义，靠语境区分 |
| 2026-08-25 | 新增 `thinking-toolkit`（十个思考方法的分层路由、三组易混方法判别、串联路径与公共收口格式）和 `life-design-interview`（天赋挖掘与人生设计两个长程访谈流程、共用对话规则、三个核心概念与心理负荷边界）；README 的 Skill 数量、工具列表与状态段同步 | 原始材料是一组彼此独立的 Prompt。直接收录等于书签夹——真正会出错的地方不是执行，是选型：「我该选 A 还是 B」可能属于双向钢人、最小实验或苏格拉底提问三者之一，「我想搞懂 X」可能属于双层解释、反向拆解或纵横分析三者之一，不写死判别条件时默认会挑输出最长的那个（纵横分析，万字级），把一个概念疑问升级成一份研究报告。因此主体写成路由表，十二段原文降级到 `references/`。拆成两个包的理由是触发形态不同：前十个是单次交互的思考方法，后两个是几十轮、产出上万字、带心理负荷的访谈流程，合并会让 description 含糊、触发不准 | 用户提出并裁定拆分与补写，执行方提出路由式结构 | 本行所在提交；官方 `quick_validate.py` 对两个 Skill 均报 valid；出处署名按用户裁定移除，原署名的两个关键词在 `skills/` 与 `README.md` 下各 0 命中（本账本行不复述这两个词，避免自我证伪）；README 「六个 Skill」三处已改为八个，CHANGELOG 历史行不追溯改写。**原始材料缺 PROMPT 12·a**（人生设计的角色 / 目标 / 核心理念页），由执行方按 c、d 两页已出现的概念（四个仪表盘、指南针、重力问题、奥德赛计划、原型、失败免疫，即 Burnett 与 Evans《设计你的人生》框架）补写，并在 `references/life-design.md` 顶部和逐节标〔补写〕——**这不是原文**。两个 Skill 均无任何真实调用，状态为**尚未实测**；与其余六个不同源，规则不是从本人事故里长出来的，而是外部方法的整理，连 README「幸存者偏差」那条部分缓解也不适用 |
| 2026-08-25 | 修四处一致性缺陷：①严重度因子清单去重——`SKILL.md` 改为引用不复述，`severity-model.md` 登记唯一已知副本，跨工具模板补回丢失的第五项「证据强度」；②`project-doc-system` 的脚本调用改为 `$SKILL_DIR` 绝对路径，并把门禁脚本的用法从「就地跑」改为「复制进项目再接线」；③`work-skill/SKILL.md` 删除内嵌状态声明，改为指向 README，只保留不会过期的那句；④`work-skill` frontmatter 的 description 加引号 | ①不是隐患是已发生：权威文件列 5 个因子，两份副本都只有 4 个，丢的正是唯一管「你有多确定」的证据强度——跨工具模板因此让不支持 Skills 的 Agent 在没有证据分级的情况下定 P0–P3。②SKILL.md 写的 `node scripts/init-docs.mjs` 在安装后必然 `MODULE_NOT_FOUND`，因为 cwd 是用户项目而非 skill 目录；这是全仓唯一会硬失败的一处，第一步就报错的 skill 不会有第二步。门禁留在 skill 目录则意味着项目 CI 依赖一个不在项目仓库里的文件。③08-22 改状态时只动了 README 和事实源，SKILL.md 这份就地陈旧，且多说一句假话（宣称「开始真实试用后才能改为试用中」，实际是被判定为不需要）。④description 里 `cards: intake` 的裸冒号使整段 frontmatter 不是合法 YAML | 执行方通读全仓后提出，用户裁定修前三处；第四处为修三时在同一文件发现，一并修 | 本行所在提交；官方 `quick_validate.py` 对八个 Skill 全部报 valid——**修前 `work-skill` 是 `Invalid YAML in frontmatter`，且经 `git show HEAD:` 确认该缺陷早于本次改动，八个里仅此一个**，这意味着严格解析 frontmatter 的加载器一直无法注册它；它与 README 记载的「零次调用」是否互为因果未验证，不据此改动状态。修②后按文档写法实测：`init-docs.mjs --dry-run` 正常输出探测结果，`check-doc-index.mjs --self-test` 的 4 个负向控制全部「被破坏时确实报错」。修①后 `grep 可达性` 在 `SKILL.md` 0 命中，因子完整清单仅存于权威文件与已登记的模板副本。修③后「尚未实测」与「试用中」两个词在 `skills/` 下各 0 命中。四处均为文档改动，无任何真实批次证据，不改动任何 Skill 的状态 |
| 2026-08-25 | 状态词由六个收敛为三个：**正在使用 / 候选 / 冻结**；`尚未实测` 一律改记为 `候选`，`试用后保留`、`试用中`、`已卸载` 三个词删除。删除 `contracts/2026-08-09-workskill-canonicalization.md`，README 新增 `contracts/` 一节说明保留下来的那份是什么 | 状态词收敛：六个词里有三个从未被用到过任何一个 Skill 上（`试用后保留`、`试用中`、`已卸载`），它们只在定义里存在——按账本第 3 条修剪规则本就该降级。保留的三个覆盖真实出现过的全部状态，且新增一句判别：候选和冻结都是零调用，分它们的不是证据多少，是有没有做过「不需要」这个判断。删契约：`2026-08-09` 那份描述的迁移动作已于 2026-08-17 整体撤回，其中九处绝对路径全部指向已退役的工作机（含个人 Windows 用户名，而本仓库是 public），仓库内零处引用，路径前缀 `tooluse/` 在拆分后不存在。保留 `2026-08-08` 那份的理由与它相反：它是仓库里唯一一份完整的冻结契约成品，`acceptance-author` 讲判据怎么写，它是照着写出来的实例，有独立的样本价值 | 用户裁定（三个标签的划分与删契约的条件均由用户给出） | 本行所在提交；`git rm` 删除，内容仍在历史中可 `git show HEAD~1:` 取回。旧状态词在 `README.md` 与 `skills/` 下均 0 命中；`CHANGELOG.md` 与 `contracts/2026-08-08-hardening.md` 仍含旧词，**这是有意保留**——账本是历史记录、契约已冻结，两者都不追溯改写。`skills/work-skill/SKILL.md` 指向 README「状态词」一节的指针经核对仍有效。README 中三处引用旧词的叙述（work-skill 的降级经过、冻结与已卸载的对比、新增冻结的理由）已改写为不依赖被删词汇，且不改变原意。状态本身一个都没动：三个正在使用、四个候选、一个冻结，与改词前逐一对应。事实源 `mj-portfolio/src/data/toolShares.zh.ts` 尚未同步，本仓库暂时领先于它 |
| 2026-08-25 | `deploy-to-self-managed-server` 的 description 去掉 `Codex`、把 `Tencent Cloud` 降级为举例（Ubuntu；腾讯云、阿里云或任意 VPS），并补上真正的触发场景：托管平台已经自动重新部署了前端、自有服务器却不会跟着更新。正文加同一段说明与「输出语言跟随用户」 | 用户说明该 Skill 的真实用途是补上 Vercel 只更新前端、云端服务不动的那个缺口。`Codex` 不是触发条件而是当时的执行者，写进 description 会让别的 Agent 漏触发；厂商名同理——这个 Skill 已经因为旧名 `deploy-static-site-to-tencent` 造成过一次触发偏差（见 2026-08-11 行），description 里的厂商绑定是同一类问题的残留。而原 description 通篇没写那个真正会让人想起它的场景 | 用户裁定 | 本行所在提交；`grep Codex` 在该 SKILL.md **0 命中**；官方 `quick_validate.py` 通过。**未加入两个具体域名**：用户消息里第二个域名被截断，不据推测补写；且 Shape A / Shape B 已点名对应的两个 public 仓库，域名不增加可识别性。需要写进去时由用户给出完整域名 |
| 2026-08-25 | 核查「本机是否有真实调用证据」：结论为**没有安装、也没有触发** | 用户提出调用次数不好记录、平时直接分享 GitHub 链接给窗口使用，并回忆自动部署「用过的，在本机电脑上」。这个回忆需要核实而不是采信——README 自己写过「不能把底层机制证据冒充为 Skill 的复用与触发证据」 | 用户提出，执行方核查 | 本行所在提交；核查范围与结果：`~/.claude/skills` **不存在**，`~/.codex/skills` 下只有 `.system/skill-creator`，八个 Skill 在本机**一个都没安装**；`grep -rl` 扫 `~/.claude/projects/` 与 `~/.codex/` 命中 3 个文件，除本次会话外的 2 个（codex 2026-08-18 与 2026-08-24 归档）经逐条查看**全部是清单式描述**，零次 `SKILL.md` 读取。据此：底层部署确实做过（Actions run `31398012907`，见 2026-08-10 行），但 Skill 包从未被加载，状态维持**候选**不变。另注：README 与 2026-08-22 行所述「用户级目录联接仍指向本仓库」指的是 Windows 工作机（`New-Item -ItemType Junction`），与本 Mac 不矛盾，但意味着本机上的分发路径是分享链接而非安装 |
| 2026-08-25 | 把三个交付 Skill 与交付流程解耦：正文里 7 处步骤编号全部改写成环节名（`第⑦步（分级核验）` → `独立核验`，墙钟模板 `⑤/⑥/⑦/⑧/⑨` → `判据/实施/核验/CI/裁决`）；README 那一节由「步骤编号」改写为「交付流程（背景，不是依赖）」，开头三条例子说明无流程、已有团队流程、非交付场景各自怎么单独用 | 用户正在简化交付流程，步数会变。上一次改动（本日早些时候）给裸编号就近加标题，方向错了——那是把绑定写得更清楚，而不是解开：只要 Skill 正文里出现编号，流程一改就要回来改 Skill，而 Skill 是分发单位、流程不是。真正的解法是让 Skill 只描述自己那个环节的职责，编号留在 README 作为背景 | 用户 | 本行所在提交；`grep [①-⑪] skills/` 除 `thinking-toolkit` 自己的层级编号外 **0 命中**；八个 Skill `quick_validate.py` 全部 valid。README 保留的那张表已明确标注「正在被简化，步数会变」「权威在 deliveryFlow.zh.ts」「Skill 正文不出现编号，流程怎么变都不用回来改」。同时把「①与⑨没有自动触发器」这条改写为不依赖步数的表述——无论简化成几步，「开工前想清楚」和「最后自己裁决」都没有自动触发器 |
| 2026-08-25 | `thinking-toolkit` 由**候选**升为**正在使用** | 2026-08-25 用方法 02（双层解释法）解释本仓库本身，构成第一次真实调用。升级的直接理由是：「候选」的定义是「没有真实批次证据——只有资料核对、结构检查或虚构干跑」，这次调用之后该定义对它已为假，继续标候选会说假话 | 用户裁定 | 本行所在提交；证据为本次会话内按 `SKILL.md` 执行的一次完整调用——路由判断（识别为理解层、对象是概念而非成品或对象全貌，选定 02 而非 03/04）、两层结构、以及末尾三样（术语对应表、最容易理解错的地方、3 个检查问题）。**明确记录三项减损，不掩盖**：①「正在使用」定义含「稳定的日常使用」，一次调用不满足这一半，计入是用户裁定，**未改动定义去迁就个例**——改定义迁就实现正是 `acceptance-author` 禁止的动作；②调用者是作者本人，且发生在写出它的同一个会话里；③同日核查确认它在作者本机未安装，该次调用走的是仓库内直接读取，不是 Skill 加载器触发。`life-design-interview` 仍为候选，零调用 |
| 待办 | `acceptance-author` 的冻结规则可能需要放宽：「执行方不得新增、删除、放宽或改写判据」这一条在上游交付流程的简化中被列为可能改动项 | 用户正在其 Windows 机上优化交付流程，已知变化之一是「写判据的可能可以改了」，但方案尚未定型 | 用户 | 未处理，**且本轮明确不动**——按用户指示先按当前版本执行。记在这里只为防止遗忘：这一条是全仓最吃重的规则（三锚点、candidate 追溯、审查退回重新冻结都挂在它上面），一旦放宽，`acceptance-author`、`evidence-bound-executor`、`evidence-led-reviewer` 三份都要跟着改。上游定型后再动，不据推测预改。另记：`Andrew-JX/vault` 已由私有转为 public（2026-08-25 前的账本行记它 404，那是当时事实，不追溯改写），但其内容尚未随流程优化更新 |
