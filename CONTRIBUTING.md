# 改这个包的三条规矩

1. **不为 Skill 新增 CI、门禁脚本、vendor 副本或自动指标。** `scripts/self-check.sh` 是唯一自检脚本。
   顶层 `install.sh` 是唯一例外：它只能从固定 SHA 安装并记录版本，不得参与 CI、决策、度量或新增权限。
2. **一个 Skill 连续 3 个真实任务没改变过行为就删掉。** 不降级、不“先留着”；git 历史保留。
3. **新 Skill 先在 2 次真实任务证明「不用它会重复犯同一种错」。** 否则先记在别处，不进包。

改动理由写在 commit message，不另设变更账本；同一事实有两份，必有一份先烂。
