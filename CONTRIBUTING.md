# 改这个包的三条规矩

写下来是因为这个包差点长成一个补丁工程：门禁脚本一度比 Skill 正文还多。

1. **不为 Skill 新增 CI、脚本、vendor 副本或自动指标。** 已有的那一个自检脚本是上限。
2. **一个 Skill 连续 3 个真实任务没改变过行为就删掉**，不降级、不"先留着"。
   位置即状态：`skills/` 里的就是在用，判死的直接删，git 历史留着。
3. **新 Skill 要先在 2 次真实任务里证明「不用它会重复犯同一种错」**，否则先记在
   别处，不进包。

改动记在 commit message 里，不另设变更账本——同一件事记两处，其中一处一定会先烂。
